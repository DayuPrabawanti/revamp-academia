
mockup dashboard

candidate dari user yang memiliki user roles(dari mas madon)
trainer dari user yang memiliki user roles (dari mas madon)
nentuin on boarding sama idle dengan pemisah nya status,menggunakan counts atau const?





mockup candidat

users.users(user.photo)>users.users_email(email_address)>user.users_education(usdu_scholl,usdu_field_study)>
users.users_phones(uspo_number)>curriculum.program_entity(prog_title)>bootcamp.batch(batch_start_date


impl>repositories>services>controller>httpserver
